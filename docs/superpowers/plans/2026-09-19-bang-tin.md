# Bảng Tin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm mục "Bảng Tin" vào dashboard: admin/editor đăng bài kèm ảnh, mọi thành viên đã kích hoạt đọc và bình luận, tên tác giả lấy từ người tương ứng trong gia phả.

**Architecture:** Hai migration SQL (gắn `profiles.person_id` + bảng `news_posts`/`news_comments` với RLS và bucket riêng), một trang server component `/dashboard/news` đọc dữ liệu và ký signed URL ảnh, các client component cho danh sách/modal/bình luận, và một file server action cho thao tác ghi trên bài. Quyền được thực thi ở tầng database (RLS + trigger), giao diện chỉ ẩn/hiện nút.

**Tech Stack:** Next.js 16 (App Router, React 19), Supabase (Postgres + RLS + Storage), TypeScript, Tailwind CSS 4, framer-motion, lucide-react.

**Spec:** `docs/superpowers/specs/2026-09-19-bang-tin-design.md`

## Global Constraints

- Repo **không có test runner** (`package.json` chỉ có `dev`, `build`, `start`, `lint`, `format`). Mỗi task vì vậy bắt đầu bằng một **phép thử chạy được** (truy vấn SQL, lệnh curl vào PostgREST, hoặc thao tác trên trình duyệt) phải **thất bại trước khi viết code** và **thành công sau khi viết code**. Không được bỏ bước chạy phép thử này.
- Ngôn ngữ giao diện: mọi chuỗi hiển thị đều đi qua `t('key')`; thêm khoá song song cho cả `vi` và `en` trong `lib/i18n/messages.ts`. Không hard-code tiếng Việt trong component.
- Mọi bảng mới trong schema `public` **bắt buộc** có GRANT tường minh cho `authenticated` và `service_role`, và `REVOKE ALL ... FROM anon` (dự án Supabase sau 2026-05-30 không tự cấp quyền — xem `docs/migrations/20260904000000_add_data_api_table_grants.sql`).
- Mọi file migration mới phải được thêm vào mảng `MIGRATION_FILES` trong `app/actions/migrations.ts`, đúng thứ tự thời gian.
- Migration chạy qua trang `/dashboard/upgrade`, dùng biến môi trường `SUPABASE_DB_URL` (đã có trong `.env.local`).
- Không emoji trong code và giao diện.
- Kết thúc mỗi task: `npx tsc --noEmit` và `npx eslint <các file đã sửa>` phải sạch trước khi commit.
- Commit message tiếng Việt, kết thúc bằng dòng:
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

## Quy ước phép thử bằng SQL

Nhiều bước dùng `psql` qua `SUPABASE_DB_URL`. Mẫu lệnh dùng lại nhiều lần:

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "<câu lệnh SQL>"
```

Nếu máy không có `psql`, thay bằng:

```bash
set -a && . ./.env.local && set +a
npx --yes postgres-cli "$SUPABASE_DB_URL" -c "<SQL>" 2>/dev/null \
  || node -e "const p=require('postgres');const s=p(process.env.SUPABASE_DB_URL,{prepare:false});s.unsafe(process.argv[1]).then(r=>{console.log(JSON.stringify(r,null,2));return s.end()}).catch(e=>{console.error(e.message);process.exit(1)})" "<SQL>"
```

Phép thử ở tầng PostgREST (kiểm tra RLS đúng như người dùng thật) dùng anon key trong `.env.local`:

```bash
set -a && . ./.env.local && set +a
curl -s "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/<table>?select=*" \
  -H "apikey: $NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY"
```

---

### Task 1: Migration gắn tài khoản với người trong gia phả

**Files:**
- Create: `docs/migrations/20260919000000_link_profile_to_person.sql`
- Modify: `app/actions/migrations.ts:16-29` (mảng `MIGRATION_FILES`)

**Interfaces:**
- Consumes: `public.is_admin()`, `public.is_active_user()` (có sẵn từ `docs/schema.sql`), kiểu composite `public.admin_user_data`, hàm `public.get_admin_users()`.
- Produces:
  - Cột `public.profiles.person_id uuid` (FK `persons(id)`, `ON DELETE SET NULL`).
  - `public.current_person_id() RETURNS uuid` — trả `person_id` của `auth.uid()`; Task 4 dùng trong trigger.
  - `public.set_user_person(target_user_id uuid, target_person_id uuid) RETURNS void` — Task 3 gọi qua `supabase.rpc`.
  - `public.get_admin_users()` trả thêm cột `person_id uuid` ở cuối — Task 3 đọc.

- [ ] **Step 1: Viết phép thử — cột chưa tồn tại**

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "SELECT person_id FROM public.profiles LIMIT 1;"
```

Expected: FAIL — `ERROR: column "person_id" does not exist`.

- [ ] **Step 2: Viết migration**

Tạo `docs/migrations/20260919000000_link_profile_to_person.sql`:

```sql
-- Gắn một tài khoản đăng nhập với một người trong gia phả.
--
-- Bảng Tin cần hiển thị tên người đăng bài và người bình luận, nhưng RLS của
-- profiles chỉ cho mỗi người đọc profile của chính mình (docs/schema.sql:229),
-- nên không thể join profiles để lấy tên. Thay vào đó mỗi tài khoản trỏ tới
-- một người trong persons; bài và bình luận lưu sẵn person đó (xem
-- 20260919010000_add_news_board.sql) rồi join sang persons - bảng mà mọi
-- thành viên đều đọc được.

ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS person_id uuid REFERENCES public.persons (id) ON DELETE SET NULL;

-- Một người trong gia phả chỉ gắn với tối đa một tài khoản.
CREATE UNIQUE INDEX IF NOT EXISTS idx_profiles_person_id
  ON public.profiles (person_id)
  WHERE person_id IS NOT NULL;

-- Người đang đăng nhập trỏ tới ai trong gia phả. SECURITY DEFINER để vượt qua
-- RLS của profiles, STABLE để gọi được nhiều lần trong một câu lệnh.
CREATE OR REPLACE FUNCTION public.current_person_id()
RETURNS uuid
LANGUAGE sql
SECURITY DEFINER
STABLE
SET search_path = public, auth
AS $$
  SELECT person_id FROM public.profiles WHERE id = auth.uid();
$$;

REVOKE ALL ON FUNCTION public.current_person_id() FROM PUBLIC;
GRANT EXECUTE ON FUNCTION public.current_person_id() TO authenticated;

-- Chỉ admin được gán tài khoản với người trong gia phả, theo đúng khuôn
-- set_user_role: client không bao giờ UPDATE thẳng vào profiles.
CREATE OR REPLACE FUNCTION public.set_user_person(target_user_id uuid, target_person_id uuid)
RETURNS void
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, auth
AS $$
BEGIN
    IF NOT public.is_admin() THEN
        RAISE EXCEPTION 'Access denied.';
    END IF;

    IF target_person_id IS NOT NULL
       AND NOT EXISTS (SELECT 1 FROM public.persons WHERE id = target_person_id) THEN
        RAISE EXCEPTION 'Person not found.';
    END IF;

    IF target_person_id IS NOT NULL
       AND EXISTS (
         SELECT 1 FROM public.profiles
         WHERE person_id = target_person_id AND id <> target_user_id
       ) THEN
        RAISE EXCEPTION 'Person already linked to another account.';
    END IF;

    UPDATE public.profiles
    SET person_id = target_person_id
    WHERE id = target_user_id;
END;
$$;

REVOKE ALL ON FUNCTION public.set_user_person(uuid, uuid) FROM PUBLIC;
GRANT EXECUTE ON FUNCTION public.set_user_person(uuid, uuid) TO authenticated;

-- get_admin_users trả thêm person_id để màn hình quản trị hiện lựa chọn hiện tại.
-- Kiểu composite đang được hàm này dùng làm kiểu trả về, nên phải bỏ hàm trước
-- khi thêm thuộc tính rồi tạo lại hàm.
DROP FUNCTION IF EXISTS public.get_admin_users();

ALTER TYPE public.admin_user_data
  ADD ATTRIBUTE person_id uuid CASCADE;

CREATE OR REPLACE FUNCTION public.get_admin_users()
RETURNS SETOF public.admin_user_data
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, auth
AS $$
BEGIN
    IF NOT public.is_admin() THEN
        RAISE EXCEPTION 'Access denied.';
    END IF;

    RETURN QUERY
    SELECT au.id, au.email::text, p.role, au.created_at, p.is_active, p.person_id
    FROM auth.users au
    LEFT JOIN public.profiles p ON au.id = p.id
    ORDER BY au.created_at DESC;
END;
$$;

REVOKE ALL ON FUNCTION public.get_admin_users() FROM PUBLIC;
GRANT EXECUTE ON FUNCTION public.get_admin_users() TO authenticated;
```

Lưu ý khi chạy lại lần hai: `ALTER TYPE ... ADD ATTRIBUTE` sẽ lỗi `column "person_id" of relation "admin_user_data" already exists`. Bọc riêng khối đó cho idempotent:

```sql
DO $$
BEGIN
  ALTER TYPE public.admin_user_data ADD ATTRIBUTE person_id uuid CASCADE;
EXCEPTION
  WHEN duplicate_column THEN NULL;
END;
$$;
```

Dùng bản bọc `DO $$ ... $$` này trong file thay cho câu `ALTER TYPE` trần.

- [ ] **Step 3: Đăng ký migration**

Trong `app/actions/migrations.ts`, thêm dòng cuối mảng `MIGRATION_FILES` (sau `20260918000000_public_member_read_access.sql`):

```ts
  'docs/migrations/20260919000000_link_profile_to_person.sql'
```

- [ ] **Step 4: Chạy migration**

Mở `http://localhost:3000/dashboard/upgrade` bằng tài khoản admin và bấm chạy migration còn thiếu. (Cách khác, chạy trực tiếp: `psql "$SUPABASE_DB_URL" -f docs/migrations/20260919000000_link_profile_to_person.sql`.)

- [ ] **Step 5: Chạy lại phép thử**

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "SELECT person_id FROM public.profiles LIMIT 1;"
psql "$SUPABASE_DB_URL" -c "SELECT proname FROM pg_proc WHERE proname IN ('current_person_id','set_user_person');"
psql "$SUPABASE_DB_URL" -c "SELECT attname FROM pg_attribute WHERE attrelid='public.admin_user_data'::regtype AND attnum>0;"
```

Expected: câu 1 chạy được (0 dòng cũng đạt); câu 2 trả về 2 hàm; câu 3 có `person_id`.

- [ ] **Step 6: Kiểm tra migration idempotent**

```bash
psql "$SUPABASE_DB_URL" -f docs/migrations/20260919000000_link_profile_to_person.sql
```

Expected: chạy lần hai không lỗi.

- [ ] **Step 7: Commit**

```bash
git add docs/migrations/20260919000000_link_profile_to_person.sql app/actions/migrations.ts
git commit -m "$(cat <<'EOF'
feat: gắn tài khoản với người trong gia phả

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Kiểu dữ liệu và chuỗi ngôn ngữ

**Files:**
- Modify: `types/index.ts` (interface `Profile`, `AdminUserData`; thêm `NewsPost`, `NewsComment`)
- Modify: `lib/i18n/messages.ts` (khối `vi` quanh dòng 360 và khối `en` quanh dòng 1202)

**Interfaces:**
- Consumes: `Person` (`types/index.ts`).
- Produces: các kiểu và khoá i18n mà Task 3, 5, 6, 7 dùng:
  - `Profile.person_id: string | null`
  - `AdminUserData.person_id: string | null`
  - `NewsPost { id, title, content, image_urls, author_person_id, created_by, created_at, updated_at, author?, comment_count? }`
  - `NewsComment { id, post_id, content, author_person_id, created_by, created_at, author? }`
  - `NewsAuthor { id, full_name, gender, avatar_url }`

- [ ] **Step 1: Viết phép thử — khoá i18n chưa có**

```bash
cd /Users/nani/Workspaces/5TechSolutions/Workspaces/HoNguyen/giapha-os
grep -c "newsTitle" lib/i18n/messages.ts
```

Expected: `0`.

- [ ] **Step 2: Thêm kiểu vào `types/index.ts`**

Sửa `Profile` (thêm dòng cuối) và `AdminUserData` (thêm dòng cuối):

```ts
export interface Profile {
  id: string
  role: UserRole
  is_active: boolean
  created_at: string
  updated_at: string
  person_id: string | null
}

export interface AdminUserData {
  id: string
  email: string
  role: UserRole
  is_active: boolean
  created_at: string
  person_id: string | null
}
```

Thêm vào cuối file:

```ts
/** Người trong gia phả đứng tên một bài hoặc một bình luận. */
export interface NewsAuthor {
  id: string
  full_name: string
  gender: Gender
  avatar_url: string | null
}

export interface NewsPost {
  id: string
  title: string
  content: string
  /** Đường dẫn trong bucket `news`; trang server đổi thành signed URL khi render. */
  image_urls: string[]
  author_person_id: string | null
  created_by: string | null
  created_at: string
  updated_at: string
  author?: NewsAuthor | null
  comment_count?: number
}

export interface NewsComment {
  id: string
  post_id: string
  content: string
  author_person_id: string | null
  created_by: string | null
  created_at: string
  author?: NewsAuthor | null
}
```

- [ ] **Step 3: Thêm khoá i18n tiếng Việt**

Trong `lib/i18n/messages.ts`, ngay sau dòng `landingViewTree: 'Xem cây gia phả',` của khối `vi`:

```ts
    news: 'Bảng tin',
    newsTitle: 'Bảng tin dòng họ',
    newsDescription:
      'Thông báo, tin tức và các hoạt động chung của dòng họ.',
    newsCreate: 'Đăng bài',
    newsEdit: 'Sửa bài',
    newsDelete: 'Xoá bài',
    newsDeleteConfirm: 'Xoá bài viết này và toàn bộ bình luận của nó?',
    newsEmpty: 'Chưa có bài viết nào.',
    newsTitleLabel: 'Tiêu đề',
    newsContentLabel: 'Nội dung',
    newsImagesLabel: 'Ảnh đính kèm',
    newsTitleRequired: 'Vui lòng nhập tiêu đề.',
    newsContentRequired: 'Vui lòng nhập nội dung.',
    newsSaveError: 'Không lưu được bài viết. Vui lòng thử lại.',
    newsAccessDenied: 'Bạn không có quyền đăng bài.',
    newsComments: 'Bình luận',
    newsCommentCount: '{count} bình luận',
    newsCommentPlaceholder: 'Viết bình luận...',
    newsCommentSend: 'Gửi',
    newsCommentEmpty: 'Chưa có bình luận nào.',
    newsCommentError: 'Không gửi được bình luận. Vui lòng thử lại.',
    newsCommentDeleteConfirm: 'Xoá bình luận này?',
    newsAnonymousAuthor: 'Thành viên',
    newsLinkedPerson: 'Thành viên trong gia phả',
    newsLinkedPersonNone: 'Chưa gán',
    newsLinkedPersonError: 'Không gán được thành viên cho tài khoản này.',
```

- [ ] **Step 4: Thêm khoá i18n tiếng Anh**

Cùng vị trí tương ứng trong khối `en` (sau `landingViewTree`):

```ts
    news: 'News',
    newsTitle: 'Family news',
    newsDescription:
      'Announcements, news, and shared activities of the family.',
    newsCreate: 'New post',
    newsEdit: 'Edit post',
    newsDelete: 'Delete post',
    newsDeleteConfirm: 'Delete this post and all of its comments?',
    newsEmpty: 'No posts yet.',
    newsTitleLabel: 'Title',
    newsContentLabel: 'Content',
    newsImagesLabel: 'Photos',
    newsTitleRequired: 'Please enter a title.',
    newsContentRequired: 'Please enter the content.',
    newsSaveError: 'Could not save the post. Please try again.',
    newsAccessDenied: 'You are not allowed to post.',
    newsComments: 'Comments',
    newsCommentCount: '{count} comments',
    newsCommentPlaceholder: 'Write a comment...',
    newsCommentSend: 'Send',
    newsCommentEmpty: 'No comments yet.',
    newsCommentError: 'Could not send the comment. Please try again.',
    newsCommentDeleteConfirm: 'Delete this comment?',
    newsAnonymousAuthor: 'Member',
    newsLinkedPerson: 'Linked family member',
    newsLinkedPersonNone: 'Not linked',
    newsLinkedPersonError: 'Could not link this account to a family member.',
```

- [ ] **Step 5: Chạy lại phép thử và kiểm tra kiểu**

```bash
grep -c "newsTitle" lib/i18n/messages.ts   # mong đợi: 2 (vi và en)
npx tsc --noEmit
```

Expected: `2`, và `tsc` không báo lỗi. Nếu `tsc` báo thiếu khoá ở bản dịch nào đó, bổ sung cho khớp — kiểu `TranslationKey` được suy ra từ khối `vi`, nên hai khối phải có cùng tập khoá.

- [ ] **Step 6: Commit**

```bash
git add types/index.ts lib/i18n/messages.ts
git commit -m "$(cat <<'EOF'
feat: thêm kiểu dữ liệu và chuỗi ngôn ngữ cho Bảng Tin

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Màn hình quản trị gán tài khoản ↔ thành viên

**Files:**
- Modify: `app/actions/user.ts` (thêm `setUserPerson`)
- Modify: `app/dashboard/users/page.tsx` (nạp thêm `persons`)
- Modify: `components/AdminUserList.tsx` (thêm cột chọn thành viên)

**Interfaces:**
- Consumes: RPC `set_user_person` (Task 1), `AdminUserData.person_id` (Task 2), khoá `newsLinkedPerson*` (Task 2), component `PersonSelector` (`components/PersonSelector.tsx`, props: `persons`, `selectedId`, `onSelect`, `placeholder`, `label`, `className`).
- Produces: `setUserPerson(userId: string, personId: string | null): Promise<{ error?: string } | undefined>` trong `app/actions/user.ts`.

- [ ] **Step 1: Viết phép thử — trang quản trị chưa có ô chọn thành viên**

Chạy dev server (`npm run dev`), đăng nhập bằng tài khoản admin, mở `http://localhost:3000/dashboard/users`.

Expected: FAIL — không có ô chọn thành viên nào trong mỗi dòng tài khoản.

- [ ] **Step 2: Thêm server action**

Trong `app/actions/user.ts`, theo đúng khuôn `changeUserRole` đã có ở đầu file:

```ts
export async function setUserPerson(userId: string, personId: string | null) {
  const supabase = await getSupabase()
  const { error } = await supabase.rpc('set_user_person', {
    target_user_id: userId,
    target_person_id: personId
  })

  if (error) {
    console.error('Error linking user to person:', error)
    return { error: error.message }
  }

  revalidatePath('/dashboard/users')
}
```

(Giữ nguyên các import sẵn có của file; `getSupabase` và `revalidatePath` đã được import.)

- [ ] **Step 3: Nạp danh sách thành viên vào trang quản trị**

Trong `app/dashboard/users/page.tsx`, sau khi lấy `users`:

```ts
  const { data: persons } = await supabase
    .from('persons')
    .select('*')
    .order('full_name', { ascending: true })
```

và truyền xuống component:

```tsx
        <AdminUserList
          initialUsers={typedUsers}
          currentUserId={profile.id}
          persons={persons ?? []}
        />
```

Thêm `import { Person } from '@/types'` nếu cần cho kiểu.

- [ ] **Step 4: Thêm ô chọn vào `AdminUserList`**

Mở rộng props và thêm handler ngay cạnh `handleRoleChange`:

```tsx
interface AdminUserListProps {
  initialUsers: AdminUserData[]
  currentUserId: string
  persons: Person[]
}
```

```tsx
  const handlePersonChange = async (userId: string, personId: string | null) => {
    if (isDemo) {
      showNotification(t('adminDemoNotice'), 'info')
      return
    }
    try {
      setLoadingId(userId)
      const result = await setUserPerson(userId, personId)

      if (result?.error) {
        showNotification(t('newsLinkedPersonError'), 'error')
        return
      }

      setUsers((prev) =>
        prev.map((u) => (u.id === userId ? { ...u, person_id: personId } : u))
      )
    } finally {
      setLoadingId(null)
    }
  }
```

Trong phần render mỗi tài khoản, cạnh ô chọn vai trò, thêm:

```tsx
                  <PersonSelector
                    persons={persons}
                    selectedId={user.person_id}
                    onSelect={(id) => handlePersonChange(user.id, id)}
                    label={t('newsLinkedPerson')}
                    placeholder={t('newsLinkedPersonNone')}
                    className='w-full sm:w-64'
                  />
```

Thêm import: `import PersonSelector from '@/components/PersonSelector'`, `import { setUserPerson } from '@/app/actions/user'` (gộp vào khối import sẵn có từ `@/app/actions/user`), và `Person` vào import từ `@/types`.

- [ ] **Step 5: Kiểm tra bằng trình duyệt**

Mở lại `/dashboard/users` bằng tài khoản admin, chọn một người cho tài khoản của chính mình.

Expected: PASS — chọn xong không báo lỗi; tải lại trang thì lựa chọn vẫn còn.

Kiểm tra ở database:

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "SELECT id, person_id FROM public.profiles WHERE person_id IS NOT NULL;"
```

Expected: có ít nhất một dòng.

- [ ] **Step 6: Kiểm tra lint và kiểu**

```bash
npx tsc --noEmit
npx eslint app/actions/user.ts app/dashboard/users/page.tsx components/AdminUserList.tsx
```

Expected: không lỗi.

- [ ] **Step 7: Commit**

```bash
git add app/actions/user.ts app/dashboard/users/page.tsx components/AdminUserList.tsx
git commit -m "$(cat <<'EOF'
feat: cho phép admin gán tài khoản với người trong gia phả

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Migration bảng Bảng Tin

**Files:**
- Create: `docs/migrations/20260919010000_add_news_board.sql`
- Modify: `app/actions/migrations.ts` (mảng `MIGRATION_FILES`)

**Interfaces:**
- Consumes: `public.is_admin()`, `public.is_editor()`, `public.is_active_user()`, `public.handle_updated_at()`, `public.current_person_id()` (Task 1).
- Produces: bảng `public.news_posts`, `public.news_comments`, bucket storage `news`. Task 5, 6, 7 đọc/ghi qua PostgREST và Storage.

- [ ] **Step 1: Viết phép thử — bảng chưa tồn tại**

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "SELECT 1 FROM public.news_posts LIMIT 1;"
```

Expected: FAIL — `ERROR: relation "public.news_posts" does not exist`.

- [ ] **Step 2: Viết migration**

Tạo `docs/migrations/20260919010000_add_news_board.sql`:

```sql
-- Bảng Tin: admin/editor đăng bài, thành viên đã kích hoạt đọc và bình luận.
-- Khách chưa đăng nhập không đọc được (khác với cây gia phả), nên anon không
-- được cấp quyền gì ở đây.

CREATE TABLE IF NOT EXISTS public.news_posts (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  title text NOT NULL,
  content text NOT NULL,
  image_urls text[] NOT NULL DEFAULT '{}',
  author_person_id uuid REFERENCES public.persons (id) ON DELETE SET NULL,
  created_by uuid REFERENCES auth.users (id),
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.news_comments (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  post_id uuid NOT NULL REFERENCES public.news_posts (id) ON DELETE CASCADE,
  content text NOT NULL,
  author_person_id uuid REFERENCES public.persons (id) ON DELETE SET NULL,
  created_by uuid REFERENCES auth.users (id),
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_news_posts_created_at
  ON public.news_posts (created_at DESC);

CREATE INDEX IF NOT EXISTS idx_news_comments_post
  ON public.news_comments (post_id, created_at);

-- Tác giả do database quyết định, không phải client: chặn mạo danh.
CREATE OR REPLACE FUNCTION public.set_news_author()
RETURNS trigger
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, auth
AS $$
BEGIN
    NEW.created_by := auth.uid();
    NEW.author_person_id := public.current_person_id();
    RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS tr_news_posts_author ON public.news_posts;
CREATE TRIGGER tr_news_posts_author
  BEFORE INSERT ON public.news_posts
  FOR EACH ROW EXECUTE PROCEDURE public.set_news_author();

DROP TRIGGER IF EXISTS tr_news_comments_author ON public.news_comments;
CREATE TRIGGER tr_news_comments_author
  BEFORE INSERT ON public.news_comments
  FOR EACH ROW EXECUTE PROCEDURE public.set_news_author();

DROP TRIGGER IF EXISTS tr_news_posts_updated_at ON public.news_posts;
CREATE TRIGGER tr_news_posts_updated_at
  BEFORE UPDATE ON public.news_posts
  FOR EACH ROW EXECUTE PROCEDURE public.handle_updated_at();

ALTER TABLE public.news_posts ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.news_comments ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "Active users can view news posts" ON public.news_posts;
CREATE POLICY "Active users can view news posts" ON public.news_posts
  FOR SELECT TO authenticated USING (public.is_active_user ());

DROP POLICY IF EXISTS "Editors can insert news posts" ON public.news_posts;
CREATE POLICY "Editors can insert news posts" ON public.news_posts
  FOR INSERT TO authenticated
  WITH CHECK (
    (public.is_admin () OR public.is_editor ())
    AND auth.uid () = created_by
  );

DROP POLICY IF EXISTS "Editors can update news posts" ON public.news_posts;
CREATE POLICY "Editors can update news posts" ON public.news_posts
  FOR UPDATE TO authenticated
  USING (public.is_admin () OR public.is_editor ())
  WITH CHECK (public.is_admin () OR public.is_editor ());

DROP POLICY IF EXISTS "Editors can delete news posts" ON public.news_posts;
CREATE POLICY "Editors can delete news posts" ON public.news_posts
  FOR DELETE TO authenticated
  USING (public.is_admin () OR public.is_editor ());

DROP POLICY IF EXISTS "Active users can view news comments" ON public.news_comments;
CREATE POLICY "Active users can view news comments" ON public.news_comments
  FOR SELECT TO authenticated USING (public.is_active_user ());

DROP POLICY IF EXISTS "Active users can insert news comments" ON public.news_comments;
CREATE POLICY "Active users can insert news comments" ON public.news_comments
  FOR INSERT TO authenticated
  WITH CHECK (
    public.is_active_user ()
    AND auth.uid () = created_by
  );

-- Bình luận không sửa được: cố ý không có policy UPDATE.
DROP POLICY IF EXISTS "Authors and admins can delete news comments" ON public.news_comments;
CREATE POLICY "Authors and admins can delete news comments" ON public.news_comments
  FOR DELETE TO authenticated
  USING (
    auth.uid () = created_by
    OR public.is_admin ()
  );

REVOKE ALL ON public.news_posts FROM anon;
GRANT SELECT, INSERT, UPDATE, DELETE ON public.news_posts TO authenticated;
GRANT ALL ON public.news_posts TO service_role;

REVOKE ALL ON public.news_comments FROM anon;
GRANT SELECT, INSERT, DELETE ON public.news_comments TO authenticated;
GRANT ALL ON public.news_comments TO service_role;

INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES (
  'news', 'news', FALSE, 10485760,
  ARRAY['image/jpeg', 'image/png', 'image/gif', 'image/webp']::text[]
)
ON CONFLICT (id) DO UPDATE
SET
  public = FALSE,
  file_size_limit = 10485760,
  allowed_mime_types = ARRAY['image/jpeg', 'image/png', 'image/gif', 'image/webp']::text[];

DROP POLICY IF EXISTS "Active users can view news files" ON storage.objects;
CREATE POLICY "Active users can view news files" ON storage.objects
  FOR SELECT TO authenticated
  USING (
    bucket_id = 'news'
    AND public.is_active_user ()
  );

DROP POLICY IF EXISTS "Editors can upload news files" ON storage.objects;
CREATE POLICY "Editors can upload news files" ON storage.objects
  FOR INSERT TO authenticated
  WITH CHECK (
    bucket_id = 'news'
    AND (public.is_admin () OR public.is_editor ())
  );

DROP POLICY IF EXISTS "Editors can update news files" ON storage.objects;
CREATE POLICY "Editors can update news files" ON storage.objects
  FOR UPDATE TO authenticated
  USING (
    bucket_id = 'news'
    AND (public.is_admin () OR public.is_editor ())
  )
  WITH CHECK (
    bucket_id = 'news'
    AND (public.is_admin () OR public.is_editor ())
  );

DROP POLICY IF EXISTS "Editors can delete news files" ON storage.objects;
CREATE POLICY "Editors can delete news files" ON storage.objects
  FOR DELETE TO authenticated
  USING (
    bucket_id = 'news'
    AND (public.is_admin () OR public.is_editor ())
  );
```

- [ ] **Step 3: Đăng ký migration**

Thêm dòng cuối mảng `MIGRATION_FILES` trong `app/actions/migrations.ts`:

```ts
  'docs/migrations/20260919010000_add_news_board.sql'
```

- [ ] **Step 4: Chạy migration**

Qua `/dashboard/upgrade`, hoặc `psql "$SUPABASE_DB_URL" -f docs/migrations/20260919010000_add_news_board.sql`.

- [ ] **Step 5: Chạy lại phép thử**

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "SELECT 1 FROM public.news_posts LIMIT 1;"
psql "$SUPABASE_DB_URL" -c "SELECT tablename, policyname FROM pg_policies WHERE tablename IN ('news_posts','news_comments') ORDER BY tablename, policyname;"
psql "$SUPABASE_DB_URL" -c "SELECT id, public FROM storage.buckets WHERE id='news';"
```

Expected: câu 1 chạy được; câu 2 liệt kê 7 policy; câu 3 trả `news | f`.

- [ ] **Step 6: Kiểm tra khách không đọc được**

```bash
set -a && . ./.env.local && set +a
curl -s "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/news_posts?select=id" \
  -H "apikey: $NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY"
```

Expected: lỗi quyền (`permission denied for table news_posts`) hoặc mảng rỗng — **không** được trả về dữ liệu bài viết.

- [ ] **Step 7: Kiểm tra migration idempotent**

```bash
psql "$SUPABASE_DB_URL" -f docs/migrations/20260919010000_add_news_board.sql
```

Expected: chạy lần hai không lỗi.

- [ ] **Step 8: Commit**

```bash
git add docs/migrations/20260919010000_add_news_board.sql app/actions/migrations.ts
git commit -m "$(cat <<'EOF'
feat: thêm bảng và phân quyền cho Bảng Tin

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Trang Bảng Tin ở chế độ chỉ đọc

**Files:**
- Create: `app/dashboard/news/page.tsx`
- Create: `components/NewsClient.tsx`
- Create: `components/NewsPostCard.tsx`
- Modify: `utils/supabase/storage-path.ts` (thêm `getNewsStoragePath`)
- Modify: `components/HeaderMenu.tsx` (thêm mục điều hướng)

**Interfaces:**
- Consumes: `NewsPost`, `NewsAuthor` (Task 2), bảng `news_posts`/`news_comments` và bucket `news` (Task 4), `getProfile`/`getSupabase` (`utils/supabase/queries.ts`).
- Produces:
  - `getNewsStoragePath(value: string): string` trong `utils/supabase/storage-path.ts`.
  - `NewsClient` props: `{ posts: NewsPost[]; canPost: boolean }`.
  - `NewsPostCard` props: `{ post: NewsPost; canPost: boolean; onEdit: (post: NewsPost) => void; onDeleted: (id: string) => void }` — Task 6 và 7 dùng đúng các tên này.

- [ ] **Step 1: Viết phép thử — trang chưa tồn tại**

```bash
curl -s -o /dev/null -w "%{http_code}\n" -L http://localhost:3000/dashboard/news
```

Expected: `404`.

- [ ] **Step 2: Thêm helper đường dẫn ảnh**

Vào cuối `utils/supabase/storage-path.ts` (giữ nguyên `getGalleryStoragePath`):

```ts
/** Tách đường dẫn trong bucket `news` ra khỏi URL đã lưu, giống gallery. */
export function getNewsStoragePath(value: string): string {
  return getGalleryStoragePath(value)
}
```

- [ ] **Step 3: Viết trang server component**

Tạo `app/dashboard/news/page.tsx`:

```tsx
import NewsClient from '@/components/NewsClient'
import { getServerTranslations } from '@/lib/i18n/server'
import { NewsAuthor, NewsPost } from '@/types'
import { getProfile, getSupabase } from '@/utils/supabase/queries'
import { getNewsStoragePath } from '@/utils/supabase/storage-path'

export async function generateMetadata() {
  const { t } = await getServerTranslations()
  return {
    title: t('newsTitle'),
    description: t('newsDescription')
  }
}

export default async function NewsPage() {
  const supabase = await getSupabase()
  const profile = await getProfile()
  const canPost =
    profile?.is_active === true &&
    (profile.role === 'admin' || profile.role === 'editor')

  const { data: postsData } = await supabase
    .from('news_posts')
    .select(
      '*, author:persons!news_posts_author_person_id_fkey(id, full_name, gender, avatar_url)'
    )
    .order('created_at', { ascending: false })

  const posts = (postsData || []) as (NewsPost & { author: NewsAuthor | null })[]

  // Đếm bình luận trong một truy vấn thay vì mỗi bài một truy vấn.
  const { data: commentRows } = await supabase
    .from('news_comments')
    .select('post_id')

  const commentCount = new Map<string, number>()
  ;(commentRows || []).forEach((row: { post_id: string }) => {
    commentCount.set(row.post_id, (commentCount.get(row.post_id) ?? 0) + 1)
  })

  const signedPosts = await Promise.all(
    posts.map(async (post) => {
      const image_urls = await Promise.all(
        (post.image_urls || []).map(async (value) => {
          const { data } = await supabase.storage
            .from('news')
            .createSignedUrl(getNewsStoragePath(value), 60 * 60)
          return data?.signedUrl || ''
        })
      )

      return {
        ...post,
        image_urls: image_urls.filter(Boolean),
        comment_count: commentCount.get(post.id) ?? 0
      }
    })
  )

  return (
    <main className='relative flex w-full flex-1 flex-col overflow-auto bg-stone-50/50 pt-8'>
      <div className='relative z-10 mx-auto w-full max-w-3xl px-4 pb-12 sm:px-6 lg:px-8'>
        <NewsClient posts={signedPosts} canPost={canPost} />
      </div>
    </main>
  )
}
```

Lưu ý: tên khoá ngoại `news_posts_author_person_id_fkey` là tên Postgres tự đặt cho cột `author_person_id` ở Task 4. Nếu PostgREST báo không tìm thấy quan hệ, lấy tên thật bằng:

```bash
psql "$SUPABASE_DB_URL" -c "SELECT conname FROM pg_constraint WHERE conrelid='public.news_posts'::regclass AND contype='f';"
```

và thay vào chuỗi `select`.

- [ ] **Step 4: Viết `NewsClient`**

Tạo `components/NewsClient.tsx`:

```tsx
'use client'

import NewsPostCard from '@/components/NewsPostCard'
import { useI18n } from '@/lib/i18n/I18nProvider'
import { NewsPost } from '@/types'
import { useState } from 'react'

interface NewsClientProps {
  posts: NewsPost[]
  canPost: boolean
}

export default function NewsClient({ posts, canPost }: NewsClientProps) {
  const { t } = useI18n()
  const [items, setItems] = useState<NewsPost[]>(posts)

  const handleDeleted = (id: string) => {
    setItems((prev) => prev.filter((post) => post.id !== id))
  }

  return (
    <>
      <div className='mb-8'>
        <h1 className='title'>{t('newsTitle')}</h1>
        <p className='mt-2 text-sm text-stone-500'>{t('newsDescription')}</p>
      </div>

      {items.length === 0 ? (
        <p className='rounded-2xl border border-stone-200/60 bg-white/60 px-4 py-10 text-center text-sm text-stone-500'>
          {t('newsEmpty')}
        </p>
      ) : (
        <div className='flex flex-col gap-6'>
          {items.map((post) => (
            <NewsPostCard
              key={post.id}
              post={post}
              canPost={canPost}
              onEdit={() => {}}
              onDeleted={handleDeleted}
            />
          ))}
        </div>
      )}
    </>
  )
}
```

(`onEdit` là chỗ trống có chủ đích ở task này; Task 6 nối vào modal.)

- [ ] **Step 5: Viết `NewsPostCard`**

Tạo `components/NewsPostCard.tsx`:

```tsx
'use client'

import { useI18n } from '@/lib/i18n/I18nProvider'
import { NewsPost } from '@/types'
import Image from 'next/image'

interface NewsPostCardProps {
  post: NewsPost
  canPost: boolean
  onEdit: (post: NewsPost) => void
  onDeleted: (id: string) => void
}

export default function NewsPostCard({ post }: NewsPostCardProps) {
  const { t } = useI18n()
  const authorName = post.author?.full_name ?? t('newsAnonymousAuthor')
  const createdAt = new Date(post.created_at).toLocaleDateString('vi-VN')

  return (
    <article className='rounded-2xl border border-stone-200/60 bg-white/80 p-5 sm:p-6'>
      <header className='mb-3'>
        <h2 className='font-serif text-xl font-semibold text-stone-800'>
          {post.title}
        </h2>
        <p className='mt-1 text-xs text-stone-500'>
          {authorName} · {createdAt}
        </p>
      </header>

      <p className='text-sm leading-relaxed whitespace-pre-wrap text-stone-700'>
        {post.content}
      </p>

      {post.image_urls.length > 0 && (
        <div className='mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3'>
          {post.image_urls.map((url) => (
            <div
              key={url}
              className='relative aspect-square overflow-hidden rounded-xl bg-stone-100'>
              <Image
                src={url}
                alt={post.title}
                fill
                className='object-cover'
                sizes='(max-width: 640px) 50vw, 33vw'
              />
            </div>
          ))}
        </div>
      )}

      <footer className='mt-4 text-xs text-stone-500'>
        {t('newsCommentCount', { count: post.comment_count ?? 0 })}
      </footer>
    </article>
  )
}
```

Ảnh dùng signed URL từ tên miền Supabase, nên `next.config.ts` phải cho phép tên miền đó. Kiểm tra bằng:

```bash
grep -n "remotePatterns" -A 10 next.config.ts
```

Nếu tên miền Supabase chưa có trong danh sách, thêm vào (cùng host đang dùng cho `gallery`).

- [ ] **Step 6: Thêm mục điều hướng**

Trong `components/HeaderMenu.tsx`, thêm vào mảng `links`, ngay sau mục `/dashboard/members`:

```tsx
    {
      href: '/dashboard/news',
      icon: Newspaper,
      label: t('news'),
      hover: 'hover:bg-amber-50 hover:text-amber-700'
    },
```

và thêm `Newspaper` vào import từ `lucide-react`.

- [ ] **Step 7: Chạy lại phép thử**

```bash
curl -s -o /dev/null -w "%{http_code}\n" -L http://localhost:3000/dashboard/news
```

Expected: `200` khi đã đăng nhập; với curl không có cookie, kết quả là trang `/login` (cũng `200`) — điều này xác nhận trang được bảo vệ.

Mở trình duyệt bằng tài khoản admin: trang hiện tiêu đề "Bảng tin dòng họ" và dòng "Chưa có bài viết nào."

Thêm một bài bằng SQL để kiểm tra phần đọc:

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "INSERT INTO public.news_posts (title, content) VALUES ('Thử nghiệm', 'Nội dung thử nghiệm.');"
```

Tải lại trang: bài hiện ra, tác giả hiển thị "Thành viên" (vì insert bằng SQL không có `auth.uid()`).

- [ ] **Step 8: Dọn dữ liệu thử và kiểm tra lint**

```bash
psql "$SUPABASE_DB_URL" -c "DELETE FROM public.news_posts WHERE title='Thử nghiệm';"
npx tsc --noEmit
npx eslint app/dashboard/news/page.tsx components/NewsClient.tsx components/NewsPostCard.tsx components/HeaderMenu.tsx utils/supabase/storage-path.ts
```

Expected: không lỗi.

- [ ] **Step 9: Commit**

```bash
git add app/dashboard/news components/NewsClient.tsx components/NewsPostCard.tsx components/HeaderMenu.tsx utils/supabase/storage-path.ts
git commit -m "$(cat <<'EOF'
feat: thêm trang Bảng Tin chế độ đọc

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Đăng, sửa, xoá bài

**Files:**
- Create: `app/actions/news.ts`
- Create: `components/modal/NewsPostModal.tsx`
- Modify: `components/NewsClient.tsx` (nút "Đăng bài", mở modal)
- Modify: `components/NewsPostCard.tsx` (nút sửa/xoá khi `canPost`)
- Modify: `utils/supabase/storage.ts` (thêm `uploadNewsImage`)

**Interfaces:**
- Consumes: `NewsPost` (Task 2), bảng và bucket (Task 4), `NewsPostCard` props (Task 5).
- Produces:
  - `uploadNewsImage(file: File): Promise<{ path: string | null; error: Error | null }>`
  - `createPost(input: { title: string; content: string; imagePaths: string[] })`
  - `updatePost(input: { id: string; title: string; content: string; imagePaths: string[] })`
  - `deletePost(id: string)` — xoá cả ảnh trong bucket
  - Tất cả trả `{ error?: string } | undefined` và gọi `revalidatePath('/dashboard/news')`.

- [ ] **Step 1: Viết phép thử — chưa có nút đăng bài**

Mở `/dashboard/news` bằng tài khoản admin.

Expected: FAIL — không có nút "Đăng bài".

- [ ] **Step 2: Thêm hàm tải ảnh**

Vào cuối `utils/supabase/storage.ts`:

```ts
export async function uploadNewsImage(
  file: File
): Promise<{ path: string | null; error: Error | null }> {
  try {
    const supabase = createClient()

    const fileExt = file.name.split('.').pop()
    const fileName = `${Date.now()}-${Math.random().toString(36).substring(2, 15)}.${fileExt}`

    const { error: uploadError } = await supabase.storage
      .from('news')
      .upload(fileName, file, { cacheControl: '3600', upsert: false })

    if (uploadError) {
      throw uploadError
    }

    return { path: fileName, error: null }
  } catch (error) {
    console.error('Error uploading news image:', error)
    return { path: null, error: error as Error }
  }
}
```

- [ ] **Step 3: Viết server action**

Tạo `app/actions/news.ts`:

```ts
'use server'

import { getServerTranslations } from '@/lib/i18n/server'
import { getProfile, getSupabase } from '@/utils/supabase/queries'
import { getNewsStoragePath } from '@/utils/supabase/storage-path'
import { revalidatePath } from 'next/cache'

const UUID_PATTERN =
  /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i

async function assertCanPost() {
  const { t } = await getServerTranslations()
  const profile = await getProfile()

  if (
    !profile?.is_active ||
    (profile.role !== 'admin' && profile.role !== 'editor')
  ) {
    return { error: t('newsAccessDenied') }
  }

  return null
}

export async function createPost(input: {
  title: string
  content: string
  imagePaths: string[]
}) {
  const { t } = await getServerTranslations()
  const denied = await assertCanPost()
  if (denied) return denied

  if (!input.title.trim()) return { error: t('newsTitleRequired') }
  if (!input.content.trim()) return { error: t('newsContentRequired') }

  const supabase = await getSupabase()
  const { error } = await supabase.from('news_posts').insert({
    title: input.title.trim(),
    content: input.content.trim(),
    image_urls: input.imagePaths
  })

  if (error) {
    console.error('Error creating news post:', error)
    return { error: t('newsSaveError') }
  }

  revalidatePath('/dashboard/news')
}

export async function updatePost(input: {
  id: string
  title: string
  content: string
  imagePaths: string[]
}) {
  const { t } = await getServerTranslations()
  const denied = await assertCanPost()
  if (denied) return denied

  if (!UUID_PATTERN.test(input.id)) return { error: t('newsSaveError') }
  if (!input.title.trim()) return { error: t('newsTitleRequired') }
  if (!input.content.trim()) return { error: t('newsContentRequired') }

  const supabase = await getSupabase()
  const { error } = await supabase
    .from('news_posts')
    .update({
      title: input.title.trim(),
      content: input.content.trim(),
      image_urls: input.imagePaths
    })
    .eq('id', input.id)

  if (error) {
    console.error('Error updating news post:', error)
    return { error: t('newsSaveError') }
  }

  revalidatePath('/dashboard/news')
}

export async function deletePost(id: string) {
  const { t } = await getServerTranslations()
  const denied = await assertCanPost()
  if (denied) return denied

  if (!UUID_PATTERN.test(id)) return { error: t('newsSaveError') }

  const supabase = await getSupabase()

  const { data: post } = await supabase
    .from('news_posts')
    .select('image_urls')
    .eq('id', id)
    .single()

  const { error } = await supabase.from('news_posts').delete().eq('id', id)

  if (error) {
    console.error('Error deleting news post:', error)
    return { error: t('newsSaveError') }
  }

  const paths = (post?.image_urls || []).map((value: string) =>
    getNewsStoragePath(value)
  )

  if (paths.length > 0) {
    const { error: storageError } = await supabase.storage
      .from('news')
      .remove(paths)

    // Ảnh mồ côi không làm hỏng luồng xoá bài, chỉ ghi log.
    if (storageError) {
      console.error('Error removing news images:', storageError)
    }
  }

  revalidatePath('/dashboard/news')
}
```

- [ ] **Step 4: Viết modal soạn bài**

Tạo `components/modal/NewsPostModal.tsx` theo khuôn `components/modal/UploadModal.tsx` (khoá cuộn nền khi mở, xem trước ảnh, nút huỷ/lưu). Điểm khác: nhiều ảnh, có `textarea` cho nội dung, và gọi server action.

```tsx
'use client'

import { createPost, updatePost } from '@/app/actions/news'
import { useI18n } from '@/lib/i18n/I18nProvider'
import { NewsPost } from '@/types'
import { uploadNewsImage } from '@/utils/supabase/storage'
import { AnimatePresence, motion } from 'framer-motion'
import { Loader2, UploadCloud, X } from 'lucide-react'
import { useEffect, useState } from 'react'

interface NewsPostModalProps {
  isOpen: boolean
  onClose: () => void
  onSuccess: () => void
  initialData?: NewsPost | null
}

export default function NewsPostModal({
  isOpen,
  onClose,
  onSuccess,
  initialData
}: NewsPostModalProps) {
  const { t } = useI18n()
  const [title, setTitle] = useState('')
  const [content, setContent] = useState('')
  const [files, setFiles] = useState<File[]>([])
  const [isSaving, setIsSaving] = useState(false)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    if (isOpen) {
      setTitle(initialData?.title ?? '')
      setContent(initialData?.content ?? '')
      setFiles([])
      setError(null)
      document.body.style.overflow = 'hidden'
    } else {
      document.body.style.overflow = 'unset'
    }
    return () => {
      document.body.style.overflow = 'unset'
    }
  }, [isOpen, initialData])

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    if (!title.trim()) {
      setError(t('newsTitleRequired'))
      return
    }
    if (!content.trim()) {
      setError(t('newsContentRequired'))
      return
    }

    setIsSaving(true)
    setError(null)

    try {
      const uploaded: string[] = []
      for (const file of files) {
        const { path, error: uploadError } = await uploadNewsImage(file)
        if (uploadError || !path) {
          setError(t('newsSaveError'))
          return
        }
        uploaded.push(path)
      }

      const result = initialData
        ? await updatePost({
            id: initialData.id,
            title,
            content,
            imagePaths: uploaded
          })
        : await createPost({ title, content, imagePaths: uploaded })

      if (result?.error) {
        setError(result.error)
        return
      }

      onSuccess()
    } finally {
      setIsSaving(false)
    }
  }

  return (
    <AnimatePresence>
      {isOpen && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          className='fixed inset-0 z-50 flex items-center justify-center bg-stone-900/40 p-4'
          onClick={onClose}>
          <motion.form
            initial={{ scale: 0.96, opacity: 0 }}
            animate={{ scale: 1, opacity: 1 }}
            exit={{ scale: 0.96, opacity: 0 }}
            onClick={(e) => e.stopPropagation()}
            onSubmit={handleSubmit}
            className='w-full max-w-lg rounded-2xl bg-white p-6'>
            <div className='mb-4 flex items-center justify-between'>
              <h2 className='font-serif text-xl font-semibold text-stone-800'>
                {initialData ? t('newsEdit') : t('newsCreate')}
              </h2>
              <button type='button' onClick={onClose} aria-label={t('close')}>
                <X className='size-5 text-stone-500' />
              </button>
            </div>

            <label className='mb-1 block text-sm text-stone-600'>
              {t('newsTitleLabel')}
            </label>
            <input
              value={title}
              onChange={(e) => setTitle(e.target.value)}
              className='mb-4 w-full rounded-xl border border-stone-200 px-3 py-2 text-sm'
            />

            <label className='mb-1 block text-sm text-stone-600'>
              {t('newsContentLabel')}
            </label>
            <textarea
              value={content}
              onChange={(e) => setContent(e.target.value)}
              rows={6}
              className='mb-4 w-full rounded-xl border border-stone-200 px-3 py-2 text-sm'
            />

            <label className='mb-1 block text-sm text-stone-600'>
              {t('newsImagesLabel')}
            </label>
            <label className='mb-4 flex cursor-pointer items-center gap-2 rounded-xl border border-dashed border-stone-300 px-3 py-3 text-sm text-stone-500'>
              <UploadCloud className='size-5' />
              {files.length > 0 ? `${files.length}` : t('newsImagesLabel')}
              <input
                type='file'
                accept='image/*'
                multiple
                className='hidden'
                onChange={(e) => setFiles(Array.from(e.target.files ?? []))}
              />
            </label>

            {error && <p className='mb-3 text-sm text-rose-600'>{error}</p>}

            <button
              type='submit'
              disabled={isSaving}
              className='flex w-full items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-medium text-white disabled:opacity-60'>
              {isSaving && <Loader2 className='size-4 animate-spin' />}
              {initialData ? t('newsEdit') : t('newsCreate')}
            </button>
          </motion.form>
        </motion.div>
      )}
    </AnimatePresence>
  )
}
```

Khoá `close` đã có sẵn trong cả hai bản dịch (`lib/i18n/messages.ts:645` và `:1485`), dùng trực tiếp.

Lưu ý hành vi khi sửa bài: modal này ghi đè `image_urls` bằng đúng các ảnh vừa chọn. Nếu người dùng sửa bài mà không chọn ảnh mới, `imagePaths` là mảng rỗng và ảnh cũ bị gỡ khỏi bài. Để giữ ảnh cũ, khởi tạo `uploaded` bằng `initialData?.image_urls ?? []` **đã quy về đường dẫn bucket** bằng `getNewsStoragePath` (vì props chứa signed URL), rồi nối thêm ảnh mới. Cài đặt đúng cách này:

```ts
      const uploaded: string[] = (initialData?.image_urls ?? []).map((url) =>
        getNewsStoragePath(url)
      )
```

và thêm import `import { getNewsStoragePath } from '@/utils/supabase/storage-path'`.

- [ ] **Step 5: Nối modal vào `NewsClient`**

Trong `components/NewsClient.tsx`: thêm state `isModalOpen`, `editingPost`, nút "Đăng bài" hiện khi `canPost`, render `NewsPostModal`, và `onSuccess` gọi `router.refresh()` (`useRouter` của `next/navigation`) giống `GalleryClient`.

```tsx
  const router = useRouter()
  const [isModalOpen, setIsModalOpen] = useState(false)
  const [editingPost, setEditingPost] = useState<NewsPost | null>(null)

  const handleSuccess = () => {
    setIsModalOpen(false)
    setEditingPost(null)
    router.refresh()
  }
```

```tsx
      {canPost && (
        <button
          type='button'
          onClick={() => {
            setEditingPost(null)
            setIsModalOpen(true)
          }}
          className='mb-6 rounded-xl bg-primary px-4 py-2 text-sm font-medium text-white'>
          {t('newsCreate')}
        </button>
      )}
```

```tsx
      <NewsPostModal
        isOpen={isModalOpen}
        onClose={() => {
          setIsModalOpen(false)
          setEditingPost(null)
        }}
        onSuccess={handleSuccess}
        initialData={editingPost}
      />
```

và truyền `onEdit={(post) => { setEditingPost(post); setIsModalOpen(true) }}` xuống `NewsPostCard`.

- [ ] **Step 6: Thêm nút sửa/xoá vào `NewsPostCard`**

```tsx
  const handleDelete = async () => {
    if (!window.confirm(t('newsDeleteConfirm'))) return
    const result = await deletePost(post.id)
    if (result?.error) return
    onDeleted(post.id)
  }
```

Đặt hai nút trong `header` của thẻ bài, chỉ render khi `canPost`, gọi `onEdit(post)` và `handleDelete`. Thêm import `deletePost` từ `@/app/actions/news` và icon `Pencil`, `Trash` từ `lucide-react`.

- [ ] **Step 7: Chạy lại phép thử**

Bằng tài khoản admin trên `/dashboard/news`:

1. Bấm "Đăng bài", nhập tiêu đề + nội dung, chọn 2 ảnh, lưu → bài xuất hiện kèm ảnh, tên tác giả đúng tên người đã gán ở Task 3.
2. Bấm sửa, đổi tiêu đề, lưu mà không chọn ảnh mới → ảnh cũ vẫn còn.
3. Bấm xoá → bài biến mất.

Kiểm tra ảnh đã bị xoá khỏi bucket:

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "SELECT count(*) FROM storage.objects WHERE bucket_id='news';"
```

Expected: `0` sau khi xoá bài thử.

- [ ] **Step 8: Kiểm tra quyền ở tầng database**

Giả lập một tài khoản `member` ngay trong phiên psql để chứng minh RLS chặn, không cần đăng nhập trên trình duyệt:

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" <<'SQL'
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claims', json_build_object('sub', (SELECT id::text FROM public.profiles WHERE role='member' AND is_active LIMIT 1), 'role','authenticated')::text, true);
INSERT INTO public.news_posts (title, content) VALUES ('X','Y');
SQL
```

Expected: FAIL — `new row violates row-level security policy for table "news_posts"`. Đây là bằng chứng quyền nằm ở database chứ không chỉ ở giao diện. (Nếu không có tài khoản `member` nào, tạo một tài khoản qua `/dashboard/users` trước.)

- [ ] **Step 9: Kiểm tra lint và kiểu**

```bash
npx tsc --noEmit
npx eslint app/actions/news.ts components/modal/NewsPostModal.tsx components/NewsClient.tsx components/NewsPostCard.tsx utils/supabase/storage.ts
```

- [ ] **Step 10: Commit**

```bash
git add app/actions/news.ts components/modal/NewsPostModal.tsx components/NewsClient.tsx components/NewsPostCard.tsx utils/supabase/storage.ts
git commit -m "$(cat <<'EOF'
feat: đăng, sửa, xoá bài trên Bảng Tin

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Bình luận

**Files:**
- Create: `components/NewsComments.tsx`
- Modify: `components/NewsPostCard.tsx` (nút mở/đóng khối bình luận)
- Modify: `app/actions/news.ts` (thêm `deleteComment`)

**Interfaces:**
- Consumes: `NewsComment`, `NewsAuthor` (Task 2), bảng `news_comments` (Task 4), `useUser()` (`components/UserProvider.tsx`, cung cấp `user`, `isAdmin`, `supabase`).
- Produces: `deleteComment(id: string)` trong `app/actions/news.ts`; `NewsComments` props `{ postId: string }`.

- [ ] **Step 1: Viết phép thử — chưa mở được bình luận**

Mở `/dashboard/news` với một bài đã có.

Expected: FAIL — bấm vào số bình luận không mở ra khối nhập bình luận.

- [ ] **Step 2: Thêm action xoá bình luận**

Vào cuối `app/actions/news.ts`:

```ts
export async function deleteComment(id: string) {
  const { t } = await getServerTranslations()

  if (!UUID_PATTERN.test(id)) return { error: t('newsCommentError') }

  const profile = await getProfile()
  if (!profile?.is_active) return { error: t('newsCommentError') }

  const supabase = await getSupabase()

  // RLS quyết định ai được xoá: tác giả hoặc admin.
  const { error } = await supabase.from('news_comments').delete().eq('id', id)

  if (error) {
    console.error('Error deleting news comment:', error)
    return { error: t('newsCommentError') }
  }

  revalidatePath('/dashboard/news')
}
```

- [ ] **Step 3: Viết `NewsComments`**

Tạo `components/NewsComments.tsx`:

```tsx
'use client'

import { deleteComment } from '@/app/actions/news'
import { useUser } from '@/components/UserProvider'
import { useI18n } from '@/lib/i18n/I18nProvider'
import { NewsComment } from '@/types'
import { Loader2, Trash } from 'lucide-react'
import { useCallback, useEffect, useState } from 'react'

export default function NewsComments({ postId }: { postId: string }) {
  const { t } = useI18n()
  const { user, isAdmin, supabase } = useUser()
  const [comments, setComments] = useState<NewsComment[]>([])
  const [draft, setDraft] = useState('')
  const [loading, setLoading] = useState(true)
  const [sending, setSending] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const load = useCallback(async () => {
    setLoading(true)
    const { data, error: loadError } = await supabase
      .from('news_comments')
      .select(
        '*, author:persons!news_comments_author_person_id_fkey(id, full_name, gender, avatar_url)'
      )
      .eq('post_id', postId)
      .order('created_at', { ascending: true })

    if (loadError) {
      setError(t('newsCommentError'))
    } else {
      setComments((data || []) as NewsComment[])
    }
    setLoading(false)
  }, [postId, supabase, t])

  useEffect(() => {
    load()
  }, [load])

  const handleSend = async () => {
    if (!draft.trim()) return
    setSending(true)
    setError(null)

    // created_by và author_person_id do trigger set_news_author điền.
    const { error: insertError } = await supabase
      .from('news_comments')
      .insert({ post_id: postId, content: draft.trim() })

    if (insertError) {
      setError(t('newsCommentError'))
    } else {
      setDraft('')
      await load()
    }
    setSending(false)
  }

  const handleDelete = async (id: string) => {
    if (!window.confirm(t('newsCommentDeleteConfirm'))) return
    const result = await deleteComment(id)
    if (result?.error) {
      setError(result.error)
      return
    }
    setComments((prev) => prev.filter((c) => c.id !== id))
  }

  return (
    <div className='mt-4 border-t border-stone-200/60 pt-4'>
      {loading ? (
        <Loader2 className='size-4 animate-spin text-stone-400' />
      ) : comments.length === 0 ? (
        <p className='text-sm text-stone-500'>{t('newsCommentEmpty')}</p>
      ) : (
        <ul className='flex flex-col gap-3'>
          {comments.map((comment) => (
            <li key={comment.id} className='flex items-start justify-between gap-3'>
              <div>
                <p className='text-xs font-medium text-stone-700'>
                  {comment.author?.full_name ?? t('newsAnonymousAuthor')}
                </p>
                <p className='text-sm whitespace-pre-wrap text-stone-600'>
                  {comment.content}
                </p>
              </div>
              {(isAdmin || comment.created_by === user?.id) && (
                <button
                  type='button'
                  onClick={() => handleDelete(comment.id)}
                  aria-label={t('newsDelete')}
                  className='text-stone-400 transition-colors hover:text-rose-600'>
                  <Trash className='size-4' />
                </button>
              )}
            </li>
          ))}
        </ul>
      )}

      {error && <p className='mt-3 text-sm text-rose-600'>{error}</p>}

      <div className='mt-4 flex items-center gap-2'>
        <input
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          placeholder={t('newsCommentPlaceholder')}
          className='flex-1 rounded-xl border border-stone-200 px-3 py-2 text-sm'
        />
        <button
          type='button'
          onClick={handleSend}
          disabled={sending || !draft.trim()}
          className='rounded-xl bg-primary px-4 py-2 text-sm font-medium text-white disabled:opacity-60'>
          {t('newsCommentSend')}
        </button>
      </div>
    </div>
  )
}
```

Tên khoá ngoại `news_comments_author_person_id_fkey` kiểm tra như ở Task 5 Step 3 nếu PostgREST báo lỗi quan hệ.

- [ ] **Step 4: Nối vào `NewsPostCard`**

Thêm state `showComments`, biến footer thành nút bấm mở/đóng, và render `<NewsComments postId={post.id} />` khi mở:

```tsx
  const [showComments, setShowComments] = useState(false)
```

```tsx
      <footer className='mt-4'>
        <button
          type='button'
          onClick={() => setShowComments((prev) => !prev)}
          className='text-xs font-medium text-stone-500 transition-colors hover:text-amber-700'>
          {t('newsCommentCount', { count: post.comment_count ?? 0 })}
        </button>
        {showComments && <NewsComments postId={post.id} />}
      </footer>
```

- [ ] **Step 5: Chạy lại phép thử**

1. Tài khoản admin: mở một bài, gửi bình luận → bình luận hiện ngay, tên đúng.
2. Đăng nhập tài khoản `member` đã kích hoạt (và đã gán person ở `/dashboard/users`): gửi bình luận → thành công, tên hiện đúng.
3. Tài khoản `member` thấy nút xoá ở bình luận của mình, **không** thấy ở bình luận của admin.
4. Admin xoá được bình luận của người khác.

Kiểm tra ở database:

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "SELECT id, post_id, created_by, author_person_id FROM public.news_comments ORDER BY created_at DESC LIMIT 5;"
```

Expected: `created_by` và `author_person_id` được điền tự động (không null với tài khoản đã gán person).

- [ ] **Step 6: Kiểm tra lint và kiểu**

```bash
npx tsc --noEmit
npx eslint components/NewsComments.tsx components/NewsPostCard.tsx app/actions/news.ts
```

- [ ] **Step 7: Commit**

```bash
git add components/NewsComments.tsx components/NewsPostCard.tsx app/actions/news.ts
git commit -m "$(cat <<'EOF'
feat: thêm bình luận cho Bảng Tin

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Thẻ điều hướng ở trang dashboard và QA tổng thể

**Files:**
- Modify: `app/dashboard/page.tsx` (thêm thẻ dẫn tới Bảng Tin)

**Interfaces:**
- Consumes: khoá `news`, `newsTitle`, `newsDescription` (Task 2); trang `/dashboard/news` (Task 5).
- Produces: không có interface mới.

- [ ] **Step 1: Viết phép thử — trang dashboard chưa có thẻ Bảng Tin**

```bash
grep -c "dashboard/news" app/dashboard/page.tsx
```

Expected: `0`.

- [ ] **Step 2: Thêm thẻ**

Mở `app/dashboard/page.tsx`, tìm mảng tính năng đang render các thẻ (quanh dòng 175 và 282 có `href={feat.href}`), thêm một phần tử mới theo đúng hình dạng các phần tử đã có: `href: '/dashboard/news'`, nhãn `t('newsTitle')`, mô tả `t('newsDescription')`, icon `Newspaper` của `lucide-react`. Giữ nguyên cách đặt màu/lớp CSS của các thẻ lân cận.

- [ ] **Step 3: Chạy lại phép thử**

```bash
grep -c "dashboard/news" app/dashboard/page.tsx
```

Expected: `1`.

- [ ] **Step 4: QA toàn luồng theo mục 7 của spec**

Chạy lần lượt và ghi kết quả:

1. `/dashboard/upgrade` hiện đủ 2 migration mới ở trạng thái đã áp dụng.
2. Admin gán tài khoản của mình vào một người trong gia phả.
3. Admin đăng bài kèm 2 ảnh → hiện đúng tên tác giả, ảnh mở được.
4. Tài khoản `member` bình luận được, tên hiện đúng.
5. `member` insert thẳng vào `news_posts` bị RLS chặn (lệnh SQL ở Task 6 Step 8).
6. `member` xoá được bình luận của mình, không xoá được của người khác.
7. Khách chưa đăng nhập vào `/dashboard/news`:

```bash
curl -s -o /dev/null -w "%{url_effective} -> %{http_code}\n" -L http://localhost:3000/dashboard/news
```

Expected: chuyển hướng về `/login`.

8. Xoá bài → bình luận bị xoá theo:

```bash
set -a && . ./.env.local && set +a
psql "$SUPABASE_DB_URL" -c "SELECT count(*) FROM public.news_comments c LEFT JOIN public.news_posts p ON p.id=c.post_id WHERE p.id IS NULL;"
```

Expected: `0`.

- [ ] **Step 5: Kiểm tra toàn bộ**

```bash
npx tsc --noEmit
npx eslint .
npm run build
```

Expected: cả ba lệnh đều thành công.

- [ ] **Step 6: Commit**

```bash
git add app/dashboard/page.tsx
git commit -m "$(cat <<'EOF'
feat: thêm thẻ Bảng Tin vào trang dashboard

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Tự rà soát kế hoạch

Đối chiếu với spec `docs/superpowers/specs/2026-09-19-bang-tin-design.md`:

| Mục spec | Task |
|---|---|
| 3.1 `profiles.person_id`, `current_person_id`, `set_user_person` | Task 1 |
| 3.2 bảng, trigger, RLS, grants, bucket | Task 4 |
| 4.1 trang server + signed URL + đếm bình luận | Task 5 |
| 4.2 `NewsClient`, `NewsPostCard` | Task 5 |
| 4.2 `NewsPostModal` | Task 6 |
| 4.2 `NewsComments` | Task 7 |
| 4.3 `app/actions/news.ts` | Task 6 (bài) + Task 7 (bình luận) |
| 5. gán tài khoản ↔ thành viên | Task 3 |
| 6. điều hướng, i18n, types | Task 2 (types + i18n), Task 5 (HeaderMenu), Task 8 (thẻ dashboard) |
| 7. kiểm thử | rải trong từng task + Task 8 |
| `getNewsStoragePath` | Task 5 |

Điểm cần chú ý khi thực thi:

- `ALTER TYPE public.admin_user_data ADD ATTRIBUTE` không idempotent; Task 1 đã bọc trong khối `DO $$ ... EXCEPTION WHEN duplicate_column $$`. Phải dùng bản bọc này, không dùng câu lệnh trần.
- Props `image_urls` truyền xuống client là **signed URL**, không phải đường dẫn bucket. Mọi chỗ ghi ngược lại database phải quy về đường dẫn bằng `getNewsStoragePath` (Task 6 Step 4 đã nêu rõ).
- Tên khoá ngoại dùng trong chuỗi `select` của PostgREST (`news_posts_author_person_id_fkey`, `news_comments_author_person_id_fkey`) là tên Postgres tự sinh; nếu sai, lấy tên thật bằng truy vấn `pg_constraint` đã cho ở Task 5 Step 3.
